import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juanquijano.jqparcial',
  appName: 'jqparcial',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
